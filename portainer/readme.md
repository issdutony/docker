# Portainer

## Docker Compose

- [`portainer-compose.yaml`](portainer-compose.yaml)

## Setting

- Environment-related > Environments > local
    - Name : 可修改成自定的 Environment, e.g, `rd-lab`
    - Public IP : 可連線的 HostName 或 IP Adddress, e.g, `rd-docker` or `rd-docker.local`
